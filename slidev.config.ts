import { defineConfig } from 'slidev/cli/config'

export default defineConfig({
  // Built to dist/manual with base /SociogramaAula/manual/ via CLI flags
  // (see package.json build:manual). Hash router for GH Pages subpath.
})
